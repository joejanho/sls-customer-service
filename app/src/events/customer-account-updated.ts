import { schema } from "app/src/schemas/customer-account.schema";

export const eventName = "CustomerAccountUpdated";
export const eventSource = "com.customer-account-bank";
export const eventSchema = schema;
export const eventVersion = "1";
